import { Meta } from '@storybook/addon-docs';

<Meta
	title="Packages/source-foundations/CSS reset"
	parameters={{ previewTabs: { canvas: { hidden: true } } }}
/>

# CSS reset

Source provides an optional CSS reset that sets some sensible default CSS rules. The intention is to provide a base for a consistent environment across projects and browsers.

In a future major version, consuming the reset will become mandatory for consumers of Source.

We recommend adopting this reset today.

## Example

```tsx
import { resets } from '@guardian/source-foundations';

const document = () => (
	<html>
		<head>
			<style>{resets.resetCSS}</style>
		</head>
		<body>{/* … */}</body>
	</html>
);
```
